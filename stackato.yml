name: employees-spring-mvc-testapp-mc
instances: 1
framework:
  type: spring
services:
  mysql-java: mysql
mem: 512
app-dir: target
#hooks:
#  pre-running:
#  - unzip newrelic_agent.zip -d $STACKATO_APP_ROOT/tomcat/
#  - rm newrelic_agent.zip
#  - cd $STACKATO_APP_ROOT/tomcat/newrelic
#  - java -jar newrelic.jar install
